<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Needed to show the local IP / network name on the lobby screen. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--
      The receiver never captures audio or video, so no CAMERA or RECORD_AUDIO.
      This is deliberate: it keeps the permission prompt list empty, which
      matters because many of these panels have no working permission UI.
    -->

    <!--
      Compatibility strategy for repurposed touch panels:
      - leanback NOT required, so it installs on plain-Android devices
      - touchscreen NOT required, so it installs on true TV boxes
      - hardware acceleration and GLES are the only real requirements
    -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <!--
      ACCESS_WIFI_STATE implies a REQUIRED android.hardware.wifi feature, which
      would block installation on Ethernet-only TV boxes. The permission is
      only used to display the local IP in diagnostics, so override it to
      not-required. Same reasoning for landscape, which the activity requests.
    -->
    <uses-feature
        android:name="android.hardware.wifi"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.screen.landscape"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.PanelCast"
        android:hardwareAccelerated="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="24">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|navigation|uiMode|density|smallestScreenSize"
            android:theme="@style/Theme.PanelCast">

            <!-- Standard launcher: repurposed tablets / touch panels -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Leanback launcher: genuine Android TV home screens -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:screenOrientation="landscape"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.PanelCast" />
    </application>
</manifest>
